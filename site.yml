---
- hosts: webservers
  become: yes
  vars_files:
    - group_vars/webservers/secrets.yml
  roles:
    - nginx

  tasks:
    - name: Afficher le mot de passe (test)
      debug:
        msg: "Password = {{ admin_password }}"
